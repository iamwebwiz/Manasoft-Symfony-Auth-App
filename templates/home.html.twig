{% extends 'base.html.twig' %}

{% block title %}HOME{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-body">
                    <h3>Welcome to Manasoft!</h3>
                    <a href="{{ path('login') }}" class="btn btn-primary">Login</a>
                    <a href="{{ path('register') }}" class="btn btn-primary mx-3">Register</a>
                    <a href="" class="btn btn-success">Dashboard</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
